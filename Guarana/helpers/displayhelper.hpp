#ifndef DISPLAYHELPER_HPP
#define DISPLAYHELPER_HPP

#include <QMenu>
#include <QPoint>


class DisplayHelper
{
public:

//    static void showContextMenu(QMenu & contextMenu,
//                                const QPoint & pos,
//                                const QWidget * current,
//                                QWidget * parent)
//    {
//        QPoint p2(pos.x(), pos.y());

//        while(current != nullptr && current != parent)
//        {
//            p2.setX(p2.x() + current->x());
//            p2.setY(p2.y() + current->y());
//            current = current->parentWidget();
//        }

//        contextMenu.exec(parent->mapToGlobal(p2));
//    }

};

#endif // DISPLAYHELPER_HPP
